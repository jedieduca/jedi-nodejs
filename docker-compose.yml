services:
  node_api:
    build: .
    container_name: node_api
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - jedieduca-network

networks:
  jedieduca-network:
    external: true